# ================================
# Auth Engine (auz-engine) — Port 3000
# ================================

PORT=3000

# MongoDB connection string for the auth/user-role database
MONGO_DB_URI=mongodb+srv://aryanag2701_db_user:aryan@user-role.ra19zht.mongodb.net/User-Role
# JWT secret used to sign and verify tokens (use a long random string in production)
JWT_SECRET=your_jwt_secret_here

# URL of the Registry Service (used to resolve PermissionSets at runtime)
REGISTRY_SERVICE_URL=http://localhost:3001
