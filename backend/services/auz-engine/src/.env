# MongoDB connection (same cluster as role-service & registry-service)
MONGO_DB_URI=mongodb+srv://aryanag2701_db_user:aryan@user-role.ra19zht.mongodb.net/

# Port for this service
PORT=4000

# JWT secret — MUST match the secret used when tokens are issued
# Change this to a long random string in production
JWT_SECRET=dummy_secret_change_in_production
